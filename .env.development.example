# 本地 dev 环境变量模板
# 用法：复制本文件为 `.env.development.local`（已在 .gitignore 中），然后填入真实值。
#
# 为什么需要：原生壳会通过 URL ?t=<base64> 把 AppId/AppKey/ApiDomain 等注入给 WebView，
# 但本地浏览器直接开 https://localhost:5173/ 不会带这些参数，NET_CONFIG.ID / NET_CONFIG.KEY
# 为空 → AES 加密后接口名密文服务端解不出 → Laravel 抛 NotFoundHttpException → 404。
# 这里填了值之后，config.ts 会在 DEV 模式下把它们兜底注入到 NET_CONFIG，本地浏览器也能联通测试服。
#
# 优先级：URL ?t=xxx  >  localStorage 缓存  >  这里的 VITE_DEV_*  >  空
# 这些值**不会**打进生产包，只在 `vite dev` 运行时生效。

# —— 必填（没有这三个连不上测试服） ——
# App 识别 ID，由后端分配
VITE_DEV_APP_ID=
# App 密钥，用于 AES 加解密 + 签名，务必和 AppId 配对
VITE_DEV_APP_KEY=
# 后端接口域名（不带 /client 后缀，示例：http://vclub23.cookiegeeks.com）
VITE_DEV_API_HOST=http://vclub23.cookiegeeks.com

# —— 选填 ——
# 声网 Agora AppId（涉及 RTC / RTM 才需要）
VITE_DEV_AGORA_APP_ID=
# 本地国家码（如 CN / US）
VITE_DEV_LOCAL_CCODE=CN
# 语言（如 zh / en）
VITE_DEV_LANGUAGE=zh
# UI 版本
VITE_DEV_UIV=1
# 广告 ID
VITE_DEV_AD_ID=
