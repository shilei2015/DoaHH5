# 生产包默认不注入 VConsole（代码侧 PROD 已默认关闭；此处可显式写上便于阅读）
VITE_DEBUG_CONSOLE=false

# 原生桥标识（与 App 的 messageHandlers / window 回调名一致），默认 A0019；双端不同名时可再配 VITE_NATIVE_BRIDGE_ANDROID
# VITE_NATIVE_BRIDGE_NAME=YourBridgeName
# VITE_NATIVE_BRIDGE_ANDROID=YourAndroidBridgeName
